{
    "scripts": {
        "setup": "mkdir -p dist && cp -r src/* dist && rm -rf dist/*.js dist/*.ts dist/**/*.js dist/**/*.ts",
        "dev": "npx esbuild@latest src/app.ts --bundle --outfile=dist/app.js",
        "build": "npx esbuild@latest src/app.ts --bundle --minify --outfile=dist/app.js",
        "clean": "rm -rf dist"
    }
}